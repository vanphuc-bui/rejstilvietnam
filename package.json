{
  "name": "rejstilvietnam",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run audit:images && astro build && npm run audit:visuals && npm run audit:site && npm run audit:worker",
    "preview": "astro preview",
    "audit:images": "node scripts/audit-images.mjs",
    "audit:visuals": "node scripts/audit-rendered-visuals.mjs",
    "audit:site": "node scripts/audit-site.mjs",
    "audit:browser": "node scripts/audit-browser.mjs",
    "audit:worker": "wrangler deploy --dry-run --outdir .wrangler-dry-run"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "wrangler": "^4.0.0"
  }
}
