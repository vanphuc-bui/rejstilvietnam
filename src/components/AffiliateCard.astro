---
const { title, text, price = 'Pris vises hos partneren', href = '#', partner = 'Viator', label = 'Populært valg' } = Astro.props;
---
<div class="affiliate-card">
  <div><span class="badge">{label}</span><h3>{title}</h3><p>{text}</p><small>{price}</small></div>
  <div class="action"><span class="disclosure">Annoncelink</span><a href={href} rel="sponsored noopener" target="_blank">Se hos {partner} →</a></div>
</div>
<style>
.affiliate-card { display: flex; justify-content: space-between; gap: 24px; padding: 24px; border: 1px solid var(--border); border-radius: 22px; background: white; }
.badge { display: inline-block; padding: 5px 9px; border-radius: 999px; background: var(--green-100); color: var(--green-700); font-size: .72rem; font-weight: 850; text-transform: uppercase; letter-spacing: .06em; }
h3 { margin-top: 10px; }
p { color: var(--muted); margin: 7px 0; }
small { color: #7a857f; }
.action { min-width: 180px; display: flex; flex-direction: column; align-items: end; justify-content: center; gap: 7px; }
.disclosure { font-size: .72rem; color: #7b857f; }
.action a { padding: 11px 15px; border-radius: 999px; background: var(--green-950); color: white; font-weight: 800; font-size: .9rem; }
@media (max-width: 620px) { .affiliate-card { flex-direction: column; } .action { align-items: stretch; } .action a { text-align: center; } }
</style>
